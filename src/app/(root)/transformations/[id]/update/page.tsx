type TransformationsUpdatePageProps = Readonly<{
	params: Readonly<{
		id: string;
	}>;
}>;

export default function TransformationsUpdatePage({
	params,
}: TransformationsUpdatePageProps): React.ReactElement {
	return (
		<section>
			<h1>Transformations Update Page</h1>
		</section>
	);
}
